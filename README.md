# jChat-Client
A Swing client for my jChat project. The code is probably trash due to my spaghetti code and the NetBeans boilerplate stuff.

I used the NetBeans IDE with its visual designer, so expect a lot of spaghetti.

The username implementation is kinda early but it should work correctly
